apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: fundhub-api-dev
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: fundhub-api-dev
  policy:
    numerical:
      order: asc