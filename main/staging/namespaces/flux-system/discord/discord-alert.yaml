apiVersion: notification.toolkit.fluxcd.io/v1
kind: Alert
metadata:
  name: on-call-webapp
spec:
  summary: "cluster staging"
  providerRef:
    name: discord
  eventSeverity: info
  eventSources:
    - kind: GitRepository
      name: 'fundhub-k8s-config'
    - kind: Kustomization
      name: '*'